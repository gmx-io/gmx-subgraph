{
  "name": "gmx-referrals",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen ./subgraph-arbitrum.yaml",
    "build": "graph build ./subgraph-arbitrum.yaml",
    "deploy:fuji": "graph codegen ./subgraph-fuji.yaml && graph build ./subgraph-fuji.yaml && goldsky subgraph deploy gmx-fuji-referrals/$(git rev-parse --abbrev-ref HEAD)-$(date '+%y%m%d%H%M%S')-$(git rev-parse --short HEAD) --path .",
    "deploy:avalanche": "graph codegen ./subgraph-avalanche.yaml && graph build ./subgraph-avalanche.yaml && goldsky subgraph deploy gmx-avalanche-referrals/$(git rev-parse --abbrev-ref HEAD)-$(date '+%y%m%d%H%M%S')-$(git rev-parse --short HEAD) --path .",
    "deploy:arbitrum": "graph codegen ./subgraph-arbitrum.yaml && graph build ./subgraph-arbitrum.yaml && goldsky subgraph deploy gmx-arbitrum-referrals/$(git rev-parse --abbrev-ref HEAD)-$(date '+%y%m%d%H%M%S')-$(git rev-parse --short HEAD) --path .",
    "deploy:megaeth": "graph codegen ./subgraph-megaeth.yaml && graph build ./subgraph-megaeth.yaml && goldsky subgraph deploy gmx-megaeth-referrals/$(git rev-parse --abbrev-ref HEAD)-$(date '+%y%m%d%H%M%S')-$(git rev-parse --short HEAD) --path .",
    "deploy:all": "yarn deploy:fuji && yarn deploy:arbitrum && yarn deploy:avalanche && yarn deploy:megaeth"
  },
  "dependencies": {
    "@babel/preset-env": "^7.23.6",
    "@graphprotocol/graph-cli": "^0.21.1",
    "@graphprotocol/graph-ts": "^0.20.0",
    "@types/jest": "^29.5.11",
    "axios": "^1.6.2",
    "jest": "^29.7.0"
  },
  "devDependencies": {
    "@babel/preset-typescript": "^7.23.3",
    "@jest/globals": "^29.7.0",
    "ts-jest": "^29.1.1"
  }
}
