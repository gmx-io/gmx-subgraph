{
  "name": "gmx",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "prepare": "sh -c 'yarn codegen -- \"./subgraph-$@.yaml\" && yarn build -- \"./subgraph-$@.yaml\"' --",
    "deploy:fuji": "yarn prepare fuji && goldsky subgraph deploy \"synthetics-fuji-stats/$(git rev-parse --abbrev-ref HEAD)-$(date '+%y%m%d%H%M%S')-$(git rev-parse --short HEAD)\" --path .",
    "deploy:arbitrum": "yarn prepare arbitrum && goldsky subgraph deploy \"synthetics-arbitrum-stats/$(git rev-parse --abbrev-ref HEAD)-$(date '+%y%m%d%H%M%S')-$(git rev-parse --short HEAD)\" --path .",
    "deploy:avalanche": "yarn prepare avalanche && goldsky subgraph deploy \"synthetics-avalanche-stats/$(git rev-parse --abbrev-ref HEAD)-$(date '+%y%m%d%H%M%S')-$(git rev-parse --short HEAD)\" --path .",
    "deploy:botanix": "yarn prepare botanix && goldsky subgraph deploy \"synthetics-botanix-stats/$(git rev-parse --abbrev-ref HEAD)-$(date '+%y%m%d%H%M%S')-$(git rev-parse --short HEAD)\" --path .",
    "deploy:megaeth": "yarn prepare megaeth && goldsky subgraph deploy \"synthetics-megaeth-stats/$(git rev-parse --abbrev-ref HEAD)-$(date '+%y%m%d%H%M%S')-$(git rev-parse --short HEAD)\" --path .",
    "deploy:all": "yarn deploy-satsuma:fuji && yarn deploy-satsuma:arbitrum && yarn deploy-satsuma:avalanche"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.3",
    "@graphprotocol/graph-ts": "0.20.1",
    "ethers": "^5.4.6",
    "moment": "^2.29.1"
  }
}
