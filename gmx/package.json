{
  "name": "gmx",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service gmx-io/gmx-stats",
    "create-local": "graph create --node http://localhost:8020/ gmx",
    "remove-local": "graph remove --node http://localhost:8020/ gmx",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gmx"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0",
    "ethers": "^5.4.6",
    "moment": "^2.29.1"
  }
}
